#include "easyfind.hpp"

#include <vector>

int main( void ) {

std::vector<int> v;
v.push_back( 1 );
v.push_back( 3 );
v.push_back( 8 );
v.push_back( 2 );
v.push_back( 9 );


std::vector<int>::iterator iter = easyfind(v, 2);
if (num == v.end())


return 0;
}